import XCTest
@testable import GBVisualComponents

final class VisualComponentsTests: XCTestCase {
    func testExample() throws {
        XCTAssertEqual(GBVisualComponents().text, "Hello, World!")
    }
}
